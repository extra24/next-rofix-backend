spring:
  config:
    activate:
      on-profile: prod
  cors:
    allowed-origins: https://fitspot-front.nextcloudlab.com # 추후에 배포 도메인으로 교체
    allowed-methods: GET, POST, PUT, DELETE, OPTIONS

  datasource:
    url: ${SPRING_DATASOURCE_URL}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    defer-data-source-initialization: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        default_schema: next

#  session:
#    store-type: redis
#    timeout: 3600
#
#  redis:
#    host: fitspot-redis-cache-moypae.serverless.apn2.cache.amazonaws.com:6379
#    port: 6379
#    ssl: true

management:
  endpoints:
    web:
      exposure:
        include: health
      base-path: /